generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Guide {
  id          String @id @default(auto()) @map("_id") @db.ObjectId
  emoji       String
  title       String
  description String
  image       String
}
